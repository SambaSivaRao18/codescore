# Exclude local node_modules - npm ci installs fresh ones inside Docker
node_modules

# competition.db is INCLUDED (baked into image with existing questions & teams)

# Exclude dev/editor files
.env
.env.*
.gitignore
*.log
npm-debug.log*

# Exclude git history
.git
